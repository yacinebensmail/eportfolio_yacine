<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Mon Portfolio{% endblock %}</title>
        {# On importe Bootstrap (Bootswatch) comme demandé  #}
        <link rel="stylesheet" href="https://bootswatch.com/5/cyborg/bootstrap.min.css">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {# Barre de navigation commune #}
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
          <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_accueil') }}">Portefolio [TON NOM]</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor01">
              <ul class="navbar-nav me-auto">
                <li class="nav-item">
                  <a class="nav-link active" href="{{ path('app_accueil') }}">Home</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="{{ path('app_cv') }}">Mon CV</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="{{ path('app_portfolio') }}">Mon E-Portfolio</a>
                </li>
              </ul>
            </div>
          </div>
        </nav>

        {# Le "bloc body" est la seule partie qui changera sur chaque page #}
        <div class="container mt-4">
            {% block body %}{% endblock %}
        </div>

        {# Le pied de page, requis sur toutes les pages  #}
        <footer class="text-center text-muted mt-5 p-4">
            <p>&copy; 2025-2026 [TON NOM PRÉNOM] - BUT1 Réseaux & Télécommunications, IUT de Roanne. 
            <br>Site créé dans le cadre de la SAE1.04. Tous droits réservés.</p>
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>